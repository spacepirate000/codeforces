#include <bits/stdc++.h>
using namespace std;
 
int main() {
    int t;
    cin >> t;
 
    while (t--) {
        int n, m, d;
        cin >> n >> m >> d;
 
        int maxH = d / m + 1;
        int towers = (n + maxH - 1) / maxH;
 
        cout << towers << '\n';
    }
 
    return 0;
}
